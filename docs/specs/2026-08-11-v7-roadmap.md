# OSRT v7 — Project Review and Roadmap

**Date:** 2026-08-11
**Status:** Roadmap / investigation — **nothing implemented.** (No longer
"nothing approved": §14 commits a model shape as of 2026-08-12.)
**⚠ Amended 2026-08-12 — read §12 and §13 first.** An independent citation
audit found no fabricated sources but three material errors, one of which
(§12.1-C2) removes the evidential basis for the mHC decision as §7.3-C/§9-G1
frame it. **§13 then changes the north star** — highest quality at fastest
inference, on Blackwell — which voids §10's NVFP4 rejection, adds gate G7,
promotes decode work out of Tier 4, and settles mHC as off permanently.
**§14 then commits a shape** (~993M, 28 x h2112 top-4, mHC off), superseding
§8's strawman and adding gate G3a. **§15 adds the DSpark evidence** — it
reopens the MTP head count, retires §7.7's autoregressive loop-3 draft, and
adds G8, which runs against the frozen v6 checkpoint and blocks nothing. §0–§11 are preserved unedited; §12 carries
the corrections, §13 the requirements change, §14 the committed shape.
This note reviews the v6 lineage to date, scans the mid-2026 frontier
(DeepSeek V4, GLM lineage, Kimi K3, NVIDIA Nemotron, plus smaller research
models), and proposes a ranked candidate set for what "v7" should be.
**Companions:** `docs/AGENT_HANDOFF.md` (live v6 state),
`docs/specs/2026-07-26-precision-and-sft-objective.md` (§11 open items, §12
rejected list, §14 opportunity catalogue — this note builds on, and does not
re-litigate, those), `docs/specs/2026-06-16-cross-loop-kv-reuse.md` (recursion
probe), `RESEARCH.md` (bibliography through ~June 2026), `LEARNINGS.md` (v5).

---

## 0. TL;DR

| question | answer |
|---|---|
| Where is v6 really? | **Pipeline-complete** — midtrain3 (12,600 steps → ~1× Chinchilla), SFT v3/v4, GRPO v6/v6b + EMA + soup all banked on HF; but the repo docs/configs lag the runs and results need pulling from W&B. §2.0 |
| Did the v6 bets hold up? | Yes, remarkably: Muon-hybrid, QK-norm>QK-Clip, aux-loss-free, sqrt(softplus), SwiGLU clamp, MTP, OPD are all now frontier-validated (V4 1.6T, Inkling 975B, K3 2.8T). The recursion remains unique to OSRT — and our probe shows it does real work. §2.1, §4–§6 |
| What is v7? | **Not a re-architecture.** A re-grain (32×h960 top-8 experts), re-tokenize (adopt a teacher's tokenizer → unlock logit KD), cost-fix (mHC ≤5% overhead or off), and recipe upgrade (WSD + ladder + KD anneal + RL hygiene kit) on the same recursive core, sized to sit comfortably on one H100/RTX PRO 6000. §7–§8 |
| Biggest single lever? | **The tokenizer swap** — it converts the token-starved budget into a distillation-dense one (~32× denser signal/token via top-K logit KD from a same-tokenizer teacher). §7.3-B |
| User requirements honoured? | (1) single-GPU comfort: strawman ~574M phys / ~253M active, ~55–58GB peak; (2) more smaller experts: 32×960 top-8 iso-param re-grain; (3) tokenizer replaced pending bake-off; (4) Sinkhorn cost capped or mHC dropped. §3.1, §7.3, §8 |
| Boldest idea worth one experiment? | **Engram sidecar memory** (hashed N-gram lookup): attacks the knowledge gap of a small-active model at ~0 FLOPs and composes with recursion (loops compose, memory remembers). §7.4 |
| What decides everything? | A **50–150M scaling ladder + µP transfer** on the always-on box — it re-derives the token target for a weight-reused sparse model AND A/Bs every candidate before the trunk run. §9 |
| First three actions? | G0 hygiene (land missing configs, stream-position log, WSD switch), G1 mHC cost probe, G2 tokenizer bake-off — all ≈ $0. §9 |

---

## 1. Method

Two inputs feed this roadmap:

1. **An internal review** of everything the v5→v6 lineage has proven,
   disproven, or left open — from the repo's own docs, probes, and training
   history (§2–§3).
2. **An external scan** of what moved at the frontier between the last
   RESEARCH.md refresh (~June 2026) and today, focused on the model families
   the user named: DeepSeek V4, GLM 5.x, Kimi K3, the latest NVIDIA Nemotron
   releases, and the "Inkling" line (§4–§6).

The synthesis (§7–§9) applies the repo's standing filter: **measure before you
build; tokens are the bottleneck; a lever is only worth taking if it survives
contact with the ~601M-scale budget reality.** v7 should be the smallest set of
changes that converts what v6 *proved* into a model that actually *performs* —
not a grab-bag of 2026 features.

---

## 2. v6 report card — what the project has actually established

### 2.0 Where the pipeline actually is (2026-08-11, verified against HF)

`docs/AGENT_HANDOFF.md` (2026-07-06) describes midtrain3 as "no surviving
checkpoint yet". That is now stale. The private checkpoint repo
(`HallD/osrt-v6-ckpt`) shows the **entire §8 roadmap of the handoff has been
executed end-to-end** since then:

| stage | artifact evidence | notes |
|---|---|---|
| midtrain3 | `osrt_v5_midtrain3_step_12600.pt`, `_final.pt`, rescue at 12038 | the full 12,600-step / +3.4B-token plan → base is at ~1× Chinchilla (~5.6B tokens) |
| SFT v3 | `osrt_v5_sft_v3_final.pt`, `data/sft_v3.jsonl` (~207MB) | post-midtrain3 SFT rebuild |
| SFT v4 | `osrt_v5_sft_v4_final.pt`, `_step_1600.pt`, `data/sft_v4.jsonl` + val split | second SFT iteration |
| soup | `osrt_v5_sft_v4_soup_1200_1400_1600_1800.pt` | checkpoint-soup polish stage reached |
| GRPO v6 | `grpo_v6_step_10..400.pt`, `data/grpo_prompts.jsonl` | RL stage ran ≥400 steps |
| GRPO v6b | `grpo_v6b_*`, `grpo_v6b_s7x200_*` + `*_ema_*` | second RL round, **with weight-EMA** (a §14.3 recommendation, evidently adopted) |

Two consequences for this roadmap:

1. **v6 is pipeline-complete.** Pretrain → midtrain×3 → SFT×4 → GRPO×2 →
   soup all exist. Whatever the final GSM8K/eval numbers say (they live in
   W&B, run `codhe-synextra/osrt` — not readable from this session; confirm
   before locking v7 decisions), the v6 *program* has run its course. This is
   the right moment to plan v7 rather than keep extending v6 ad hoc.
2. **The runs have outpaced the committed code.** No `SFTv3Config` /
   `SFTv4Config` / `grpo_v6b` / soup configs exist in `src/osrt/train_config.py`
   on `main`, and `CLAUDE.md`/`AGENT_HANDOFF.md` describe a two-cycles-old
   state. Whatever drove sft_v3/v4 and grpo_v6/v6b lives outside git.
   **v7 hygiene item #1: land those configs + a results table before the
   details evaporate** — this repo's whole methodology depends on the paper
   trail.

### 2.1 Proven (keep; these are the assets)

- **The architecture trains, and the recursion is real.** The full stack —
  3 physical blocks × 6 loops (18 effective layers), 1 shared + 8 routed
  top-2 experts, GQA 24q/8kv over an MLA-style compressed-latent KDV cache,
  rank-256 native HRA per effective layer, Muon+AdamW hybrid — pretrained,
  midtrained (×2), and SFT'd without loop collapse or router collapse.
  The 2026-06-16 probe (`docs/specs/2026-06-16-cross-loop-kv-reuse.md` §6)
  showed per-loop KV latents are *distinct* and adjacent-loop CKA movement
  contracts monotonically (block 0: 0.30 → 0.17 → 0.07 → 0.05 → 0.05) — a
  **contracting fixed-point iteration**, i.e. non-degenerate iterative
  refinement. This is the single most important scientific result the project
  owns: the differentiator (sparse MoE + depth recurrence + Muon) is not a
  degenerate combination.
- **The stability stack works.** QK-norm, sandwich RMSNorm, per-loop routing
  accounting, balance-bias controller + Switch aux + z-loss, SwiGLU clamp,
  aux-loop losses (0.05) + loop dropout: v5's three signature failures (loop
  collapse, router collapse, logit blow-up) did not recur in v6 training.
- **The training infra is genuinely portable.** One `run_training` loop with
  three front-ends (Modal / Colab / bare CUDA), cross-session chaining via HF
  checkpoint sync (`scripts/hf_ckpt_sync.py`), W&B as the reliable monitor.
  The nine landmines in `AGENT_HANDOFF.md` §5 are all diagnosed and fenced.
  Tree is green as of this note: 215 passed / 1 skipped on CPU.
- **Format teaching works.** SFT v2 produced `format_ok = 1.0` — clean
  `<|think|>…<|/think|><|answer|>…<|/answer|>`, no loop-collapse generation.
  Eliciting *format* from this base is a solved problem.
- **Compute-bound identity confirmed.** The architecture runs at ~69% of H100
  throughput on an RTX PRO 6000 with ~half the bandwidth — recursion reuses
  weights, KDV shrinks the cache. The design thesis (spend bandwidth savings
  on compute) held on real hardware.

### 2.2 Disproven or failed (v7 must not repeat)

- **"SFT/GRPO can rescue an undertrained base" — disproven.** GSM8K stayed at
  ~0.04–0.06 across SFT v1 and v2 despite a verified-reasoning corpus. The
  diagnosis (`AGENT_HANDOFF.md` §2): the base has seen **~2.2B tokens ≈ 0.4×
  Chinchilla for 278M active params** — models this size that reason saw
  trillions. Post-training elicits; it does not create.
- **Cross-loop KV reuse — rejected by measurement** (mean injected rel-L2
  ≈ 1.04; loops carry distinct KV). The 6× bandwidth prize is off the table
  for this trained model; any future attempt must be *trained-in*, not
  retrofitted.
- **Attention sink — dropped** (memory blow-up at seq 8192, no demonstrated
  benefit).
- **The token-budget arithmetic was the plan's weakest link.** v6's design doc
  targeted 1.5–3T tokens; the funded reality delivered ~2.2B before midtrain3.
  The gap between the design's data ambition and the drip budget is the
  defining constraint of the whole project, and v7 planning has to start from
  it rather than rediscover it at the end.
- **mHC's Sinkhorn mixing was a measured throughput tax** (user report,
  2026-08-11: tok/s was "really really slow until optimised", with Sinkhorn
  the main suspect). Each effective layer runs **two** mHC sub-blocks —
  attention and FFN (`model.py:1370`, `:1379`) — so a forward performs
  **36 Sinkhorn projections × 20 iterations each**
  (`mhc_sinkhorn_iters=20`) when 3–5 typically converge — the 2026-07-26
  note had already flagged profiling it (§11). Combined with the unresolved NaN question (§2.3.1), mHC in its
  current form is on notice: v7 either makes it cheap (3–5 iters, or a
  cheaper non-expansive constraint) or drops the 4-channel stream entirely.

### 2.3 Open / unresolved (v7's cheapest wins live here)

From `2026-07-26-precision-and-sft-objective.md` §11/§14 and README §13, still
open as of this note:

1. **mHC at scale** — the preset ships `use_mhc=True` but CPU pre-flight saw
   gradient amplification + NaN under sustained training
   (`presets.py:38-42`); the GPU A/B (`n_hc ∈ {4,2,off}`) has not run. v6 is
   *carrying an unvalidated stability risk in its default config.*
2. **Chinchilla is ill-defined for a 6×-weight-reused sparse model** — the
   scaling ladder + µP transfer (§14.3) that would pin the real token target
   has not run. Every budget decision inherits this uncertainty.
3. **Cosine → WSD (trunk-and-branch)** for drip training — catalogued, not
   adopted; every extension still pays the re-warm tax.
4. **Data fast-forward across resumed sessions** — possible silent
   stream-head oversampling; one W&B log line would settle it (§14.1).
5. **Speculative decoding via the loop-3 draft** — acceptance rate never
   measured; per-loop self-distillation (§14.5) that would raise it not
   started.
6. **Loops × blocks trade-off** (3×6 vs 4×4 vs 2×8 at fixed compute) — never
   ablated; the probe result (convergence by loops 4–5) *suggests* the last
   loops are cheap to trim, but only at representation level, not logits.
7. **Logit-level KD is blocked by the custom 65K tokenizer** — flagged in
   §14.9(b) as "a real cost of the tokenizer worth weighing in any v7
   decision."
8. **Recent opt-in features await A/B:** SiTU-GLU (Kimi K3 §2.3.2 —
   `config.py:67`, param-free activation swap) and per-head Muon (Kimi K3
   §2.5 — `muon.py:90`) are implemented, tested, default-off, unmeasured.

### 2.4 Standing constraints (unchanged by any v7 ambition)

- **Budget:** ~$120/month Modal drip + Colab units + one always-on GPU box.
  1× Chinchilla for the current model ≈ 3 months of drip.
- **Deliverable:** a working architecture + pipeline + as much pretraining as
  budget allows — not a frontier model.
- **Naming debt:** repo/preset names (605M/A288M/A269M) predate the corrected
  601M/278M count; `compute_budget.py` is the only trusted source.

---

## 3. The strategic question v7 must answer

v6's bottleneck is not architecture — it is **tokens through the machine**.
Any v7 that increases cost-per-token without either (a) increasing
learning-per-token or (b) unlocking cheaper tokens is a step backwards. That
framing sorts every candidate change into three buckets:

- **B1. More learning per token** — data quality/curriculum, distillation,
  MTP evolution, objective changes, optimizer efficiency.
- **B2. More tokens per dollar** — throughput (fp8/precision, loop
  curriculum, seq-length policy), scheduler (WSD → no re-warm waste),
  stream hygiene (no oversampling), free/granted compute.
- **B3. More capability per parameter at deploy time** — attention/KV
  efficiency, spec decode, early exit, quantization.

The external scan below is read through exactly this lens.

### 3.1 Hard requirements set by the user (2026-08-11)

These are requirements, not candidates — every §7 proposal must satisfy
them:

1. **Single-GPU training fit, comfortably.** v7 trains on **one H100 80GB
   or one RTX PRO 6000 96GB**. v6 needed ~58GB at batch-6/seq-4096 with
   grad checkpointing — workable but tight, and it forced micro-batch
   gymnastics on 40GB cards. "Comfortably" means sizing the v7 model +
   optimizer + activations so the H100 case has real headroom (target:
   ≤~60GB at the standard recipe, leaving room for bigger batch, longer
   seq, or in-loop eval), and the RTX PRO 6000 (compute-rich,
   bandwidth-poor — proven at ~69% of H100 on this arch) stays a
   first-class target. Consequence: **total params stay in the 600M–1B
   band**; the shape can change, the memory envelope cannot.
2. **Revisit expert granularity: more, smaller experts.** v6 chose 8×3840
   (top-2, 25% density) deliberately deferring the fine-grained thesis to
   "16B+ scale" — the user now wants that reopened. The 2026 evidence is
   on their side: every scanned family is fine-grained (V4-Flash 256
   experts top-6; Nemotron Nano 128 top-6 **at only 3.2B active**; Inkling
   256+2 top-6; see also §5). Fine-grained also un-defers the §8 adaptive
   top-k idea (rejected at E=8, explicitly deferred to E≥32).
3. **The tokenizer is not good enough.** First-hand quality complaint, on
   top of the already-documented structural cost: the custom 65K BPE
   **blocks logit-level KD from every open teacher** (2026-07-26 note
   §14.9b). v7 should treat the tokenizer as replaceable, and weigh
   adopting a proven open tokenizer whose teacher family we would
   distill from (§7).
4. **The residual stream must not tax throughput.** Sinkhorn-20 across 36
   projections per forward (2 mHC sub-blocks × 18 effective layers) was a
   measured tok/s drag (§2.2). Whatever v7 does about mHC — fewer
   iterations, cheaper constraint, or removal — the residual stream
   returns to near-zero overhead.

---

## 4. External scan: DeepSeek V4 lineage

Researched 2026-08-11. RESEARCH.md §2 was written against the **April preview
+ HF card**; the **full V4 tech report is now out (arXiv 2606.19348, Apr
2026)**, plus two research threads (mHC paper, Engram) and a Flash refresh
(V4-Flash-0731, Jul 2026). R2 never shipped — the R-series was absorbed into
V-series reasoning-effort modes (Non-think / Think-High / Think-Max).

### 4.1 What the full report confirms (that v6 already bet on)

OSRT v6 adopted several V4-preview features on faith; the report now
validates each at 1.6T-param scale, with exact numbers:

- **mHC, n_hc=4, 20 Sinkhorn iterations** — production config (mHC paper:
  arXiv 2512.24880; −0.021 final loss at 27B, +2.1 BBH). **Critically: the
  6.7% time overhead DeepSeek quotes is *after* kernel fusion + recompute +
  DualPipe engineering.** OSRT has none of that — which squares with the
  user's observation that Sinkhorn dominated tok/s until optimised. The
  constraint is validated; *our implementation cost* is the problem (§7).
- **Muon exact recipe:** Nesterov 0.95, wd 0.1, update-RMS 0.18 rescale,
  hybrid Newton–Schulz 10 iters (8 fast + 2 stabilizing (2,−1.5,0.5)),
  AdamW (ε=1e-20) for embeddings/head/norms **and mHC static
  biases/gates** — worth auditing `build_param_groups` for that last split.
  Notably **no QK-Clip**: RMSNorm on Q/KV entries suffices (matches OSRT's
  QK-norm choice).
- **sqrt(softplus) router affinity, aux-loss-free bias speed 0.001, plus a
  small sequence-wise balance loss at 1e-4** (OSRT has the field
  `router_seq_balance_loss_coeff=0.0` — V4 says turn it on, tiny).
- **Hash routing for the first 3 MoE layers** (token-ID hash) — OSRT's
  `hash_routing_blocks` exists but ships off.
- **MTP depth-1, loss 0.3 → 0.1 at LR decay** — OSRT runs 2 heads at 0.3
  flat; the anneal-to-0.1 refinement is free.
- **SwiGLU clamp ±10** — exactly OSRT's `swiglu_clamp=10.0`.
- **Anticipatory routing is a spike-*recovery* mode, not always-on** —
  engaged around detected loss spikes with stale-parameter routing, then
  reverted (~20% overhead while active). Pairs with the §14.3(b) spike
  auto-rollback idea.

### 4.2 What's genuinely new since RESEARCH.md

- **CSA/HCA in full detail:** the lightning indexer now scores **4×
  sequence-compressed** KV entries (FP4 QK), top-k=512/1024 compressed
  entries, MQA over shared latent entries, **plus a parallel 128-token
  sliding-window branch + attention sink**, RMSNorm on Q/KV, partial RoPE
  (last 64 dims). MLA per-layer latent KV survives only as DNA (low-rank Q,
  latent-style shared entries). Sequence-dim compression replaced
  channel-dim compression as the KV lever.
- **Engram (arXiv 2601.07372, Jan 2026) — the verified "DeepSeek engram":**
  an O(1) **hashed N-gram embedding lookup** (2–3-grams, multiplicative-XOR
  multi-head hashing) + context-aware gating, inserted at 1–2 shallow
  layers. Headline: a **U-shaped sparsity-allocation law — reallocating
  ~20–25% of the sparse budget from MoE experts to lookup memory wins at
  iso-params/iso-FLOPs** (MMLU +3.0, GSM8K +2.2, BBH +5.0 at 27B), and CKA
  shows shallow Engram layers doing the work of much deeper MoE layers
  ("effective depth" gain). Table can offload to host DRAM at ≤2.8%
  throughput cost. **Not in V4** (named as future work there).
- **xHC (arXiv 2607.14530, third-party, Jul 2026):** diagnoses mHC's n>4
  saturation (single write-back starves streams; O(n³C) mapping cost) and
  fixes it with temporal-conv write-back augmentation + k-of-N sparse
  stream updates. Also: mHC-as-PEFT (2607.18130) — adapting residual
  mixers instead of weights, adjacent to HRA.
- **V3.2's MoE-RL stabilizer kit** (carried into V4's specialists): **Keep
  Routing** (cache sampling-time expert paths, enforce them during policy
  updates — "crucial" for MoE RL), unbiased K3 KL estimator, off-policy
  negative-sequence masking, keep-sampling-mask.
- **OPD is now the whole consolidation stage:** specialists via SFT+GRPO,
  then **reverse-KL full-vocabulary distillation on the student's own
  rollouts** unifies them (the README §10b.1 plan, now the shipped V4
  pipeline — and independently NVIDIA Ultra's "MOPD", §6.1).

### 4.3 What this means for OSRT

1. Every "adopted on faith" v6 feature survived scale contact — the
   *choices* were right; the open problems are cost (mHC) and measurement.
2. **Engram is the single most interesting new idea for a recursive
   model:** loops spend shared capacity reconstructing static facts every
   pass; a hashed lookup sidecar supplies facts for free and lets the loops
   do composition. It attacks exactly the knowledge-vs-skill trade
   (§6.2/§6.3) from the memory side, at near-zero FLOPs.
3. The **V4 attention pattern is scale-independent**: compressed-global +
   sliding-window + sink over the existing latent cache would cut
   long-context cost ~4× without the indexer (indexer only pays >16K).
4. **MoE-RL hygiene (Keep Routing et al.)** plugs straight into the
   existing GRPO stack and may explain past RL instability cheaply.

## 5. External scan: GLM 5 / 5.2 (Zhipu) and Kimi K3 (Moonshot)

Researched 2026-08-11. These two labs matter doubly to OSRT: they are the
production home of the Muon lineage, and the repo has *already* imported two
K3 report features (SiTU-GLU §2.3.2, per-head Muon §2.5) behind default-off
flags.

### 5.1 GLM-5 (Feb 2026, 744B/A40B, arXiv 2602.15763) and GLM-5.2 (Jun 2026)

- **Switched GQA → MLA ("MLA-256")** and closed the Muon-vs-MLA gap with
  **"Muon Split"**: the fused MLA up-projections are split per attention
  head and Newton–Schulz runs on each head's momentum separately. Their
  ablation says this per-head treatment is *what makes* Muon+MLA match or
  beat GQA — the strongest possible endorsement of the repo's new
  `per-head Muon` flag (same idea, independently derived as K3 §2.5).
- **Adopted DeepSeek's DSA** (lightning indexer) on top of MLA; GLM-5.2
  adds **IndexShare** (one indexer reused across every 4 sparse layers)
  for a usable 1M context at 2.9× lower per-token indexing cost.
- **Ablated and REJECTED cheap linear/sliding-window attention:**
  RULER@128K 75.3 dense vs 44.9 SWA-interleave, 67.0 SimpleGDN. The
  warning for OSRT: cut-down linear attention loses retrieval — hybrids
  only work with the *full* KDA-style recipe (see §5.2).
- **MTP with parameter sharing across 3 MTP layers** (fixes the V3
  train/inference discrepancy); spec-decode acceptance 2.76 tokens.
- Post-training: SFT → reasoning RL (GRPO + IcePop mismatch correction;
  **DSA indexer frozen during RL**) → async agentic RL (token-in/token-out
  gateway, double-sided importance sampling) → general RL → **on-policy
  cross-stage distillation** to consolidate — *policy-space* consolidation,
  explicitly instead of weight merging. RL infra (`slime`) is Apache-2.0.
- GLM-5.2 was assessed as the strongest open-weights model at release
  (first open model at 50 on the AA index was GLM-5). All MIT.
- Small-model note: GLM-4.7-Flash (30B-A3B, Jan 2026, MIT) is their
  current small MoE; no GLM-5-generation small model yet.

### 5.2 Kimi K3 (Jul 2026, 2.8T/A104B, arXiv 2607.24653) and the K2 lineage

- **Hybrid KDA:MLA at 3:1** (69 KDA + 24 gated-MLA layers): KDA = delta
  rule + channel-wise forget gate + lower-bounded log-decay + sigmoid
  output gate + short conv, from the Kimi Linear paper (2510.26692 —
  1.16× compute-optimal vs full-MLA, 6× decode @1M, with a full ablation
  grid at 48B-A3B). **Gated MLA layers use NoPE — no RoPE anywhere**; the
  decay gates carry position.
- **Stable LatentMoE:** 896 routed experts top-16 + 2 shared (~1.8%
  routed density); routed experts run in a **compact latent space** with
  an **RMSNorm between expert aggregation and up-projection** — a one-line
  routed-branch stabilizer. (Same latent-experts idea as Nemotron's
  LatentMoE, §6.1 — two labs converged.)
- **SiTU-GLU** — the smooth β·tanh(x/β) cap on gate and up branches
  (already in this repo behind `situ_glu`).
- **Quantile Balancing (QB):** aux-loss-free balancing where the
  per-expert bias is set **directly from the router-score quantile
  matching the target load** (histogram over the global batch) —
  deterministic, hyperparameter-free, "zero dead experts". A drop-in
  upgrade candidate for OSRT's heuristic ±γ bias controller, and most
  valuable exactly where OSRT lives (small expert pools, where one dead
  expert = 12.5% of capacity).
- **Per-head Muon** (≡ GLM Muon Split), **MuonClip retired**: K2's QK-Clip
  is gone in favour of norm-based control. Independently, **QK-Normed MLA
  (Ant Group, arXiv 2606.16310) validates QK-norm > QK-Clip at 400M
  params / 100B tokens** — OSRT-scale evidence for OSRT's existing choice,
  plus an exact factorization for keeping QK-norm inside a latent decode
  path at ~1.4% overhead.
- **QAT from SFT onward: MXFP4 weights / MXFP8 activations** — all
  reported benchmarks are at deployed precision.
- Post-training: SFT → RL over **nine specialists (3 domains × 3
  reasoning-effort levels)** → **MOPD** (multi-teacher on-policy
  distillation, clipped per-token teacher–student log-prob reward) into
  one model. MTP head is fine-tuned into an EAGLE-3-style draft model.
- Third-party MOPD paper (Xiaomi, 2606.30406): **weight merging measured
  worst-in-class for consolidating specialists** (0.33–0.86 normalized vs
  MOPD 0.94); same-origin teachers (shared SFT init) are the stability
  precondition. Note this does *not* contradict Nemotron's +2–4pt
  checkpoint merging (§6.1) — that merges a sliding window of *the same
  run*; what's dead is merging *different specialist models*.

### 5.3 What this pair means for OSRT

1. **Graduate the two flags now justified at scale:** per-head Muon
   (GLM says it's the reason Muon+MLA works) and SiTU-GLU (K3's expert
   activation). Both are implemented and tested — they need an A/B, not
   engineering.
2. **Quantile Balancing** is the single best new router idea for small-E
   MoE — deterministic and hyperparameter-free where OSRT currently tunes
   `update_rate/ema_rate/max`.
3. **The fine-grained + latent experts direction is unanimous** (K3 896,
   GLM 256, V4 256–384, Nemotron 128–512, Inkling 256) — supports the
   user's "more smaller experts" requirement; latent-space experts are the
   param-efficiency variant worth one ladder slot.
4. **Hybrid linear attention is real but all-or-nothing** — Kimi Linear's
   full recipe wins; GLM's ablation shows half-measures lose retrieval.
   At OSRT's 4–8K target context this is a stretch experiment, not a
   default (§7).
5. **Consolidation is policy-space, not weight-space** — for v7's
   multi-domain post-training, plan OPD/MOPD-style distillation between
   same-origin specialists; keep same-run checkpoint souping.
6. **NoPE + decay-gated position** is how two labs now do length
   generalisation; OSRT's RoPE remains fine at 8K, but this removes any
   pressure to adopt exotic RoPE scaling in v7.

## 6. External scan: NVIDIA Nemotron 3 and Inkling (Thinking Machines)

Researched 2026-08-11 (web + alphaXiv sweep; primary sources cited).
RESEARCH.md §12 covered Nemotron-CC and noted "Nemotron 3 Ultra" only as a
too-slow teacher; the picture is now much bigger.

### 6.1 Nemotron 3 family (Dec 2025 → Jul 2026) — confirmed

Nano 30B-A3B (arXiv 2512.20848), Super 120B-A12B (2604.12374), Ultra
550B-A55B (2606.15007), family overview (2512.20856), Nano Omni (2604.24954),
Puzzle-75B-A9B compression (2607.04371). All open-weights (Nemotron Open
Model License) with **>10T tokens of the training data released openly** —
the most complete open release at frontier-adjacent scale.

Architecture:
- **Hybrid Mamba-2 + sparse-attention layout.** Nano keeps only **6
  full-attention (GQA) layers out of 52** as "global anchors"; Mamba-2
  carries the sequence elsewhere. **No positional embeddings at all** —
  Mamba supplies position implicitly, which is how 1M-context extension
  avoids RoPE OOD. 3.3× decode throughput vs Qwen3-30B-A3B.
- **MoE everywhere else:** 128 experts top-6 (Nano), aux-loss-free
  balancing, squared-ReLU experts, untied embeddings.
- **LatentMoE (Super/Ultra):** route + run experts in a down-projected
  latent space; reinvest the saved bandwidth into ~4× more experts at the
  same inference cost. Beats iso-cost standard MoE on every eval reported.
- **MTP heads** (shared-weight): +~2.4% avg quality at ablation scale and
  ~97% 2-token draft acceptance for self-speculative decoding; "MTP
  healing" post-stages retrain the head on-policy.

Training:
- **NVFP4 end-to-end pretraining is production reality** (Super: 25T tokens;
  Ultra: 20T; recipe in arXiv 2509.25149 — keep last ~15% of layers +
  QKV/out projections high-precision, Hadamard transforms + stochastic
  rounding). Requires Blackwell; loss gap <1% vs BF16.
- **WSD with a two-phase curriculum** (broad diversity ~90–95% of tokens →
  high-quality/synthetic anneal) across the whole family.
- **Checkpoint merging during the stable phase: +2–4 points avg across 12
  benchmarks** — weighted averaging over a sliding window of checkpoints.
- **Iterative Puzzle compression:** (prune expert channels → reduce top-k →
  shrink SSM state) × 3 rounds, each healed by short KD; iterative beats
  one-shot; ~2× throughput at near-parent accuracy.

Post-training:
- SFT → **simultaneous multi-environment RLVR** (staged RL was *less*
  stable and more reward-hackable), GRPO with masked importance sampling,
  **MoE router frozen during RL**, difficulty-based curriculum sampling.
- Ultra adds **MOPD — multi-teacher on-policy distillation** (>10 domain
  specialists, reverse-KL on student rollouts, PPO-style clipping) — the
  same pattern README §10b.1 planned from the V4 preview, now productionised
  by NVIDIA.
- **Reasoning on/off + token-budget control trained purely via SFT data**
  (model completes answers from truncated thinking traces); Ultra's
  medium-effort mode: ~2.5× fewer tokens for ~7% accuracy drop. RLHF adds
  group-relative **length-control reward: −30% verbosity at no accuracy
  cost**.

### 6.2 Inkling / Inkling-Small (Thinking Machines Lab) — confirmed

"Inkling" = the open-weights multimodal MoE family from Thinking Machines
(Mira Murati's lab): **Inkling** 975B/A41B (2026-07-15, Apache-2.0) and
**Inkling-Small** 276B/A12B (2026-07-30). Blog + model card only, no arXiv
report. Headline facts:

- **Muon + Adam hybrid at 975B/45T-token scale** — exactly OSRT's optimizer
  split (Muon on large 2D matrices, Adam elsewhere), with one refinement:
  **weight decay coupled ∝ LR²** so decay follows the schedule. This is the
  strongest at-scale validation the Muon-hybrid bet has received.
- Attention: **5:1 sliding-window:global interleave**, 8 KV heads,
  **relative positions instead of RoPE** (length extrapolation), short
  convolutions (SConv) on attention projections and residual branches.
- MoE: 256 routed + 2 shared experts, top-6, sigmoid router,
  DeepSeek-style aux-loss-free bias.
- MTP layers for self-speculative decoding; encoder-free multimodality;
  7-level `reasoning_effort` dial; async RL at 30M+ rollouts.
- **Inkling-Small beats its own 975B parent on agentic/skill evals**
  (SWE-bench Verified 80.2 vs 77.6, ARC-AGI-2 40.1 vs 36.5) while losing
  −23 pts on SimpleQA factuality — the cleanest public datapoint that
  **small-active models trade knowledge, not skill**. Debuted as the
  leading US open-weights family on the AA index (41 vs Nemotron Ultra 38).

### 6.3 What this pair means for OSRT

1. The **optimizer bet is settled** (Inkling), and the **WD ∝ LR² coupling**
   is a free recipe upgrade to trial.
2. The **skill-vs-knowledge trade** (Inkling-Small) is directly encouraging
   for a 278M-active reasoning-first model: prioritise reasoning/agentic
   density in the anneal, delegate facts to tools/retrieval.
3. **Attention is being deleted, not just compressed** — both labs converge
   on "a few global-attention anchors + cheap sequence mixing" (Mamba-2 or
   SWA+SConv). OSRT's recursive analog: make attention heterogeneous
   *across the 3 physical blocks* (e.g. one full-attention anchor block +
   two cheap-mixer blocks), which multiplies through the 6 loops.
4. **Checkpoint merging in the stable phase** and **reasoning-budget
   control via SFT data** are near-zero-cost adoptions.
5. **Router frozen during RL + simultaneous multi-env RLVR + length-control
   reward** are directly applicable to the existing GRPO stack.
6. NVFP4 pretraining is real but Blackwell-only — park for v8; selective
   FP8 PTQ (keep attention anchors BF16) is the deployment-quant pattern to
   copy now.

---

## 7. Candidate set for v7 (ranked)

One striking meta-result of the scan: **v6's bets aged well.** Muon-hybrid,
QK-norm-over-QK-Clip, aux-loss-free balancing, sqrt(softplus), SwiGLU clamp,
MTP, fine-grained-MoE-direction, OPD — every one is now validated at
frontier scale, several by multiple labs independently. v7 is therefore not
a re-architecture; it is a **re-grain + re-tokenize + cost-fix + recipe
upgrade** on a proven core. The recursion (3 blocks × N loops) remains the
differentiator — still no released model combines sparse MoE with depth
recurrence.

### 7.1 Tier 0 — process and hygiene (do regardless; ~$0)

| # | item | why |
|---|---|---|
| 0.1 | Land sft_v3/v4, grpo_v6/v6b, soup configs + a results table; unstale `CLAUDE.md`/`AGENT_HANDOFF.md` | §2.0 — runs outpaced the repo; the methodology *is* the paper trail |
| 0.2 | **WSD trunk-and-branch** for every future long run | already catalogued (§14.2 of 2026-07-26 note); now also the unanimous 2026 practice (Nemotron two-phase WSD, Kimi Linear WSD) |
| 0.3 | Verify cross-session stream fast-forward (one W&B line) | §14.1 — protects every drip dollar |
| 0.4 | **Same-run checkpoint merging** in the stable phase | Nemotron: +2–4 pts avg, free; v6's SFT soup already validated locally |
| 0.5 | Spike auto-rollback; optionally V4-style stale-router recovery around spikes | §14.3(b) + V4's anticipatory routing as *recovery mode*, not always-on |
| 0.6 | Weight EMA as default for eval/release ckpts | +2.4GB; grpo_v6b already used EMA |

### 7.2 Tier 1 — cheap graduations and config deltas (hours each; A/B on the ladder)

| # | item | evidence |
|---|---|---|
| 1.1 | **Graduate per-head Muon** (`muon.py:90`, default-on candidate) | K3 §2.5 + GLM-5 "Muon Split" — GLM's ablation credits it for Muon+MLA ≥ GQA |
| 1.2 | **Graduate SiTU-GLU** (replaces hard clamp) | K3 §2.3.2; differentiable where clamp is flat |
| 1.3 | Align Muon to the V4 recipe: hybrid NS (8 fast + 2 stabilizing), update-RMS 0.18, Nesterov; audit AdamW group to include mHC statics; trial **WD ∝ LR²** | V4 report (exact recipe); Inkling (WD coupling) |
| 1.4 | **Quantile Balancing** router bias vs current ±γ heuristic | K3; deterministic, no hyperparameters, "zero dead experts" |
| 1.5 | `router_seq_balance_loss_coeff = 1e-4` (field exists, ships 0) | V4 runs exactly this |
| 1.6 | MTP: anneal loss 0.3 → 0.1 at LR decay; consider GLM-style shared-parameter MTP | V4 / GLM-5 |
| 1.7 | Hash-routing A/B on **physical block 0** (field exists, ships 0). NOTE: `hash_routing_blocks` gates whole blocks at construction (`model.py:276`), so block 0 hash-routes in **all 6 loops**; a first-loop-only variant needs a small loop-gated extension | V4 hash-routes first 3 MoE layers |
| 1.8 | Normalize aggregated routed-expert output before it rejoins the stream | K3 "Normalized LatentMoE" pattern, adapted |

### 7.3 Tier 2 — the four headline moves (this IS v7; each behind a gate, §9)

**A. Fine-grained expert re-grain (user requirement 2).**
Iso-param, iso-active re-slice of the routed pool per block:
`8 × h3840, top-2` → **`32 × h960, top-8`** (identical 141.6M routed
params/block and identical active FLOPs; 4× finer routing at the same 25%
density). Conservative fallback: `16 × h1920, top-4`. Pair with QB
balancing (1.4) and per-loop routing telemetry (already built). Un-defers
the E≥32 world; **adaptive top-k stays deferred** — the fixed-shape/compile
objections (§12 of 2026-07-26 note) still hold. Optional ladder slot:
K3/Nemotron-style **latent-space experts** (route + compute in a ~half-width
latent) as a param-efficiency variant. Risk to measure: smaller per-expert
GEMMs → lower arithmetic intensity; grouped-GEMM must be re-benchmarked at
E=32 on both target GPUs.

**B. Tokenizer replacement (user requirement 3) — the single biggest
strategic unlock.**
The custom 65K BPE has a first-hand quality complaint AND blocks
logit-level KD from every open teacher (§14.9b). Candidates:

| option | vocab | tied-embed @1536 | unlocks logit-KD from | notes |
|---|---|---|---|---|
| keep custom | 65,536 | 100.7M | nobody | status quo; quality complaint stands |
| retrain custom on better corpus | 65,536 | 100.7M | nobody | fixes quality, not the KD blockade |
| **SmolLM3's tokenizer** | 49,152 | **75.5M (−25M)** | SmolLM3-3B (Apache-2.0, strong open reasoning teacher, dual think/no_think contract similar to ours) | frees ~25M params for experts/Engram; verify digit handling, fertility on our math/code mix, special-token mapping |
| LFM2's tokenizer | 65,536 | 100.7M | LFM2 family (350M–2.6B) | drop-in size (v6's ratios were modelled on LFM2 anyway); teacher family weaker at reasoning than SmolLM3-3B |

Recommendation to test first: **SmolLM3** — pick the teacher, then take its
tokenizer; the point is dense distillation (LFM2-style decoupled top-K KD
gives ~32× denser supervision per token — exactly what a token-starved
budget needs, and it was README §10a plan blocked only by the tokenizer).
Both candidate tokenizers need the bake-off in §9-G2 before any decision.
Consequence either way: v7 restarts pretraining — which is the honest
moment to also fix the §3.1 requirements in one go.

**C. Residual stream: make mHC cheap or turn it off (user requirement 4).**
Options in order of preference: (i) `mhc_sinkhorn_iters` 20 → 3–5 (mHC
paper itself notes typical convergence; DeepSeek's 6.7% overhead came
*with* fused kernels we don't have); (ii) replace Sinkhorn with row-softmax
mixing (drops the doubly-stochastic guarantee; keeps multi-stream); (iii)
`use_mhc=False` — v5 proved plain residual + sandwich norms survives 18
effective layers. Decision rule in §9-G1: **≤5% measured tok/s overhead on
the RTX PRO 6000 or mHC is off in v7.** Watch xHC (2607.14530) for the
n>4 fix, and mHC-as-PEFT (2607.18130) as a later serving idea.

**D. Single-GPU sizing envelope (user requirement 1).**
Hold total in the **575–700M band**, active ~250–280M, seq 4096, and
require the standard recipe to fit **≤~60GB** (batch ≥6, grad-ckpt) so H100
has headroom and RTX PRO 6000 gains batch room. The §8 strawman lands at
~574M/~57GB. Levers if tight: mHC off (frees activation stream), micro-batch
knobs (proven), fused-CE already in place.

### 7.4 Tier 2½ — one bold architecture experiment (single ladder slot)

**Engram sidecar memory (DeepSeek, 2601.07372).** Hashed N-gram lookup +
gated injection at 1–2 shallow points. Why it fits OSRT specifically: the
U-shaped allocation law says sparse budget is better split between experts
and lookup memory; the effective-depth result (shallow Engram ≈ deep MoE
layers) *composes with recursion* — loops stop re-deriving static facts and
spend depth on composition; and Inkling-Small's skill-vs-knowledge trade
(§6.2) says knowledge is exactly what a 250–280M-active model lacks.
Options: reinvest the ~25M embedding savings on-GPU, or a larger
CPU-offloaded table (paper: ≤2.8% throughput cost). **Do after the
tokenizer decision** (N-gram IDs are tokenizer-dependent). Gate: ladder A/B
at ~150M scale vs iso-param expert-only control.

### 7.5 Tier 3 — training recipe

1. **Scaling ladder + µP transfer first** (§14.3 of the 2026-07-26 note,
   now upgraded from "opportunity" to **the gating instrument for every
   Tier-1/2 choice**): ~3 sizes (50/100/150M) × 3 token budgets on the
   always-on box; re-derives the Chinchilla yardstick for a 6×-weight-reused
   sparse model before the next three months of spend.
2. **Two-phase WSD data curriculum** (diversity ~90% → high-quality anneal)
   with the **refreshed Nemotron open sets** (CC-v2.1, CC-Code-v1,
   Pretraining-Code-v2, Specialized-v1, Post-Training 3.0) — the v6 mix
   already leans Nemotron; v2 sets are bigger and cleaner. FineWeb-Edu
   `int_score≥4` filter; deliberate ~4-epoch reuse of best slices.
3. **Anneal-phase logit KD from the same-tokenizer teacher** (unblocked by
   7.3-B): decoupled top-K KD at ~32× denser signal/token; measured on the
   ladder first (G5).
4. **Reasoning-density up, knowledge via memory/tools** — the
   Inkling-Small datapoint (§6.2) licenses leaning the anneal further into
   math/code/reasoning; SimpleQA-style knowledge is Engram's and the tool
   registry's job, not the experts'.
5. **Reasoning-effort control via SFT data** (Nemotron / K3 / V4
   convergence): extend the reasoning-on/off contract to
   {off, low, high} by including truncated-trace completions — no
   architecture change, composes with the existing system-prompt contract.

### 7.6 Tier 3½ — post-training

1. **MoE-RL hygiene kit** (V4/V3.2 + GLM + Nemotron convergence): **Keep
   Routing** (enforce sampling-time expert paths during updates), **router
   frozen during RL**, keep-sampling-mask, unbiased-KL estimator,
   off-policy negative masking, deterministic top-k in rollouts.
2. **Simultaneous multi-env RLVR with difficulty curriculum** (Nemotron
   found staged RL less stable and more hackable) + **group-relative
   length-control reward** (−30% verbosity free).
3. **Consolidate specialists via OPD/MOPD, never weight-merge them**
   (V4, K3, GLM cross-stage distillation, Xiaomi MOPD numbers). Same-run
   souping stays (0.4).
4. **QAT from SFT onward on routed experts** (K2-Thinking→K3 pattern),
   reporting all evals at deployed precision; extends the existing int4-KV
   + AlphaQ deployment story.

### 7.7 Tier 4 — deployment (mostly unchanged from v6 plans)

Spec-decode via loop-3 draft (measure acceptance at last), per-loop
self-distillation to raise it (§14.5), early-exit harvest (§14.6),
selective FP8 PTQ keeping attention anchors BF16 (Nemotron pattern),
AlphaQ + TurboQuant as before. NVFP4 *pretraining* parked for v8
(Blackwell-only).

---

## 8. Proposed v7 shape (strawman — for the ladder to confirm, not a commitment)

```
dim 1536, 3 physical blocks × 6 loops (18 effective layers)   [unchanged]
GQA 24q/8kv head 64, KDV compressed-latent cache, QK-norm     [unchanged]
RoPE @ 4096 ctx (8K-capable)                                  [unchanged]
Routed experts: 32 × h960 per block, top-8 (25% density)      [re-grained]
Shared expert:  1 × h2816 per block                           [unchanged]
Router: sqrt(softplus) + Quantile-Balancing bias + z-loss
        + seq-balance 1e-4                                    [QB new]
HRA rank 256, native, per effective layer                     [unchanged]
mHC: off, OR n_hc=4 @ 3–5 Sinkhorn iters if it clears G1      [cost-fixed]
SiTU-GLU experts; per-head Muon; V4 Muon recipe               [graduated]
MTP: 1 shared-param head, loss 0.3 → 0.1 at decay             [slimmed]
Tokenizer: SmolLM3 49,152 (pending G2 bake-off)               [replaced]
Optional: Engram sidecar @ ~25M GPU / larger CPU-offloaded    [gated]
```

Parameter arithmetic (same formulas as `compute_budget.py`; regenerate
there before locking anything):

| component | v6 | v7 strawman |
|---|---|---|
| Tied embedding | 100.7M | **75.5M** (49,152 × 1536) |
| Attention × 3 | 17.3M | 17.3M |
| Shared experts | 38.9M | 38.9M |
| Routed experts | 424.7M (3×8×h3840) | 424.7M (3×32×h960) |
| HRA | 14.2M | 14.2M |
| MTP | 4.7M (2 heads) | ~2.4M (1 shared) |
| mHC + router + misc | ~1.0M | ~0.2–1.1M |
| **Total physical** | **601M** | **~574M** (+~25M Engram option → ~600M) |
| **Active/token** | **278M** | **~253M** |

Memory check against requirement 1 (fp32 masters + grads, fp32 Muon
momentum, AdamW on embed/norms): optimizer+weights floor ~7.2GB (v6:
~7.6GB); activations at batch 6 × seq 4096 with grad-ckpt ≈ v6's (same
dim/loops; less if mHC off) → **~55–58GB peak = comfortable on H100 80GB,
roomy on RTX PRO 6000 96GB** (batch 8+ or in-loop eval headroom). The
freed embedding params are the Engram budget — or stay unspent as margin.

Loops × blocks: 3×6 stays the default; the ladder gets one 4×4 and one
3×5-with-loop-curriculum slot (§14.4 loop-count curriculum; the KV probe's
"converged by loops 4–5" finding motivates both).

---

## 9. Decision gates (measure-first, in spend order)

| gate | question | method | cost | decision rule |
|---|---|---|---|---|
| **G0** | hygiene | land configs/results; stream-position log line; WSD trunk switch | ~0 | unconditional |
| **G1** | mHC fate | tok/s × {n_hc 4, 2, off} × {iters 20, 5, 3} on RTX PRO 6000 + H100; NaN repro watch | hours | ≤5% overhead and no NaN → keep cheap mHC; else off |
| **G2** | tokenizer | fertility bake-off (custom vs SmolLM3 vs LFM2) on the real mix incl. math digits + code; special-token mapping; teacher-KD dry run | ~0 (CPU) | best fertility-×-teacher package wins; any swap ⇒ v7 pretrains from scratch |
| **G3** | expert re-grain | ladder A/B at ~150M: 8×top2 vs 16×top4 vs 32×top8 (+1 latent-experts slot), QB vs heuristic bias, SiTU vs clamp, per-head Muon on/off | ladder weeks (always-on box) | best loss-per-FLOP + healthy per-loop routing wins |
| **G4** | loops×blocks | same ladder: 3×6 vs 4×4 vs 3×5+curriculum | shared with G3 | fixed-compute loss + loop_depth_probe |
| **G5** | KD payoff | anneal-KD from chosen teacher vs plain CE on the 150M proxy | days | adopt if per-token win visible; sets anneal design |
| **G6** | assemble | freeze `OSRT_V7` preset from G1–G5; compute_budget; sanity; WSD trunk pretrain on the single-GPU box; branch-decay releases | the real spend | — |

The ladder (G3/G4) is the centrepiece: it simultaneously answers the §2.3.2
Chinchilla-yardstick question, µP-transfers LRs, and de-risks every
architecture choice before the trunk run. This is the repo's own
"measure before you build" rule applied to v7 wholesale.

---

## 10. What v7 is NOT

- **Not a context-length race.** 1M-token tricks (DSA indexers, IndexShare,
  HCA) solve problems OSRT does not have at 4–8K. Revisit only if the
  north-star changes; V4's SWA+compressed-global pattern is the noted
  route (§4.3.3).
- **Not a hybrid-SSM rewrite.** KDA/Mamba-2 hybrids are real wins at 128K+
  contexts, but GLM's ablation shows half-recipes lose retrieval, the full
  recipe is a kernel-engineering project, and at 8K the payoff is small.
  One stretch slot *may* try a KDA block inside the recursion — only if the
  ladder is ahead of schedule.
- **Not NVFP4/FP8 pretraining.** Blackwell-only (NVFP4) or blocked by
  `_grouped_mm` + shape economics (FP8, §12 of the 2026-07-26 note — that
  rejection stands).
- **Not adaptive top-k.** E=32 makes it *eligible*; the fixed-shape
  compile objection still wins. Re-read §8 of the 2026-07-26 note first.
- **Not multimodal, not agent-swarm, not a bigger model.** The budget
  reality (§2.4) is unchanged; v7's job is to make the same-class model
  *good*, with a cleaner path for tokens to become capability.
- **Not weight-merging of specialists** (policy-space consolidation only).

---

## 11. Sources

Primary (papers/reports): DeepSeek-V4 tech report arXiv:2606.19348 · V3.2
arXiv:2512.02556 · mHC arXiv:2512.24880 · Engram arXiv:2601.07372 · GLM-5
arXiv:2602.15763 · GLM-4.5 arXiv:2508.06471 · Kimi K3 arXiv:2607.24653 ·
Kimi K2/MuonClip arXiv:2507.20534 · Kimi Linear arXiv:2510.26692 ·
Moonlight arXiv:2502.16982 · K2.5 arXiv:2602.02276 · Nemotron 3 family
arXiv:2512.20856 · Nano arXiv:2512.20848 · Super arXiv:2604.12374 · Ultra
arXiv:2606.15007 · Puzzle arXiv:2607.04371 · NVFP4 pretraining
arXiv:2509.25149 · QK-Normed MLA (Ant) arXiv:2606.16310 · MOPD (Xiaomi)
arXiv:2606.30406 · xHC arXiv:2607.14530 · mHC-as-PEFT arXiv:2607.18130 ·
tokenizer-agnostic Engram arXiv:2607.29065.

Official cards/blogs: DeepSeek-V4-Pro/Flash HF cards + model-card PDF ·
zai-org/GLM-5 & GLM-5.2 HF · moonshotai Kimi-K3 GitHub/HF · NVIDIA
Nemotron 3 newsroom/dev-blog/HF cards · Thinking Machines Inkling &
Inkling-Small blogs/model cards/HF.

Secondary (flagged as such in §4–§6): Artificial Analysis, Interconnects,
Simon Willison, MarkTechPost, VentureBeat, and per-claim links in the
research briefs; anything rumor-grade (GLM-5.5, R2 specs, K3 token count)
is quarantined in the scan sections and must not drive decisions.

Internal: `docs/AGENT_HANDOFF.md` · `docs/specs/2026-07-26-*.md` ·
`docs/specs/2026-06-16-cross-loop-kv-reuse.md` · `LEARNINGS.md` ·
`RESEARCH.md` · `src/osrt/{config,presets,muon,model}.py` ·
`HallD/osrt-v6-ckpt` file listing (2026-08-11).

---

## 12. Citation and content verification (2026-08-12)

**Status:** independent audit of §4–§6 and §11 against the primary sources.
Added the day after the note was written; §0–§11 are unchanged above.
Read this section before acting on any external claim in §4–§6.

### 12.0 Method and coverage

Every arXiv ID in §11 was resolved against arxiv.org. For the six sources
that carry decision weight — mHC, Engram, Kimi K3, GLM-5, DeepSeek-V4,
QK-Normed MLA — the **full text** was searched for the specific numbers §4–§6
attribute to them, not just the abstract. Internal repo claims were separately
checked against the code and `compute_budget.py` (§12.4).

**16 of 23 IDs verified directly.** The remaining seven (V3.2 2512.02556,
GLM-4.5 2508.06471, Kimi K2 2507.20534, Kimi Linear 2510.26692, Moonlight
2502.16982, NVFP4 2509.25149, Nano Omni 2604.24954) were **not re-checked in
this pass** — they are all pre-existing `RESEARCH.md` entries and none of them
carries a §7 decision on its own. Treat them as unaudited.

**No fabricated citations were found.** Every ID checked resolves to a real
paper whose subject matches the use §4–§6 makes of it. The defects below are
errors of transcription and of comparand, not of invention.

### 12.1 Corrections — claims that do not survive checking

**C1 (material). The "3–5 Sinkhorn iterations typically converge" claim is
not in the mHC paper.** §7.3-C option (i) and the §9-G1 gate both rest on it.
The paper says only:

> "We choose t_max = 20 as a practical value in our experiments."

It does not discuss convergence requirements at all. **The cheapest proposed
mHC fix is attributed to a source that does not support it.** Cutting iterations
20 → 3–5 may still work, but it is an untested hypothesis of this repo's own,
not a published finding, and must be measured before it is relied on.

**C2 (material). The +2.1 BBH gain is mHC-versus-HC, not mHC-versus-plain-
residual.** Verbatim:

> "Compared to HC, mHC further enhances the model's reasoning capabilities,
> delivering performance gains of 2.1% on BBH."

The 0.021 loss reduction appears in the same passage, about mitigating "the
training instability observed in HC", with the comparand given only as "the
baseline". So the headline benefit figures establish that **the manifold
constraint repairs Hyper-Connections** — they do not establish that a
hyper-connected residual stream beats an ordinary one. The v7 question is
mHC versus `use_mhc=False`, and **no cited source addresses it.** This is the
single most important correction in this section; see §12.3.

**C3. Engram benchmark numbers are wrong in two places.** §4.2 claims
"MMLU +3.0, GSM8K +2.2, BBH +5.0 at 27B". The paper reports MMLU **+3.4**,
CMMLU +4.0, BBH **+5.0** (correct), ARC-Challenge +3.7, HumanEval +3.0,
MATH **+2.4**. **There is no GSM8K figure** — "GSM8K +2.2" appears to be a
garbling of MATH +2.4. BBH +5.0 is exact; MMLU is understated.

**C4. The Engram paper is mis-titled.** 2601.07372 is *"Conditional Memory via
Scalable Lookup: A New Axis of Sparsity for Large Language Models"* (DeepSeek;
v1 12 Jan 2026, v2 12 Jul 2026). "Engram" is the module inside it, not the
paper. The U-shaped allocation law is confirmed verbatim.

**C5. The ">10T tokens of training data released openly" figure is not
supported by the cited source.** The Nemotron 3 family paper (2512.20856)
says only that NVIDIA will "openly release the model weights, pre- and
post-training software, recipes, and all data for which we hold redistribution
rights" — no quantity. The claim may be true from the dataset cards; the
citation given does not carry it.

**C6. "MuonClip retired" (§5.2) could not be located** in the K3 full text.
Per-head Muon is confirmed verbatim (below); the retirement claim is
unverified and should not be cited as K3's position.

**C7. Scale caveats on two supporting sources.** mHC-as-PEFT (2607.18130) is
a small academic study wrapping **frozen OLMo-2 backbones**, not a frontier-lab
result — §4.2 cites it without that context. The GLM RULER ablation (C8 below)
is run on a **GLM-9B** proxy, not on GLM-5 itself.

### 12.2 Confirmed — claims that check out verbatim

**C8. The GLM RULER@128K numbers are correct**, despite looking approximate.
Table 5 reports 64K/128K pairs; the 128K column is dense **75.28**, SWA
Interleave **44.93**, SimpleGDN **67.03** — matching §5.1's 75.3 / 44.9 / 67.0.
The conclusion (half-measure linear attention loses retrieval) stands.

Also confirmed against full text or abstract:

- **V4's 6.7% mHC overhead is post-optimisation** — fused kernels, a
  recomputation strategy, and DualPipe 1F1B adjustments, all in its §4.3.
  §2.2's central cost argument is sound.
- **V4 puts mHC static biases in the AdamW group** alongside embeddings, head
  and RMSNorm weights — the `build_param_groups` audit item in §4.1 is real.
- **V4 uses no QK-Clip** ("RMSNorm preventing attention logit explosion");
  SwiGLU clamped to [−10, 10] with the gate capped at 10; hash routing on the
  first 3 MoE layers; anticipatory routing triggered on loss spikes and
  reverted after — all exactly as §4.1 states.
- **V4 shapes:** Flash 284B/13B with 1 shared + 256 routed top-6; Pro 1.6T/49B
  with 384 routed. OPD is reverse-KL full-vocabulary distillation on the
  student's own trajectories. Reasoning modes are Non-think / Think-High /
  Think-Max.
- **K3 SiTU-GLU:** "applies the smooth cap softcap(x,β) = β·tanh(x/β) to the
  linear factor of the Swish gate and independently to the up branch."
- **K3 Quantile Balancing:** "sets each expert bias from the router-score
  quantile that matches its target load."
- **K3 per-head Muon:** "instead of applying Newton–Schulz orthogonalization to
  the full Q, K, and V projection matrices, we partition their momentum
  matrices along the head dimension."
- **K3 layout:** "Each block contains 3 KDA layers followed by 1 Gated MLA
  layer", 69 KDA + 24 MLA; shared experts fixed at N_s = 2 per layer. §5.2's
  numbers are exact.
- **GLM-5 Muon Split:** "enables projection weights for different attention
  heads to update at different scales… the method effectively improves the
  performance of MLA to match that of GQA-8." 744B / 40B active. MLA-256 is
  head dim 192 → 256 with heads cut by one third.
- **QK-Normed MLA:** "In our 400M runs trained for up to 100B tokens,
  QK-Normed MLA achieves lower training loss and better downstream accuracy
  than QK clipping" — the OSRT-scale evidence §5.2 claims, confirmed.
- **Nemotron Ultra** (550B/55B) lists LatentMoE, MTP, NVFP4 pretraining,
  multi-environment RLVR, MOPD and reasoning budget control in its abstract —
  six §6.1 claims at once. **Super** is 120B/12B, first pretrained in NVFP4,
  25T tokens.
- **xHC** diagnoses mHC saturation past N=4 via insufficient write-back and
  cubic mixing cost, and fixes it with temporal feature augmentation plus
  sparse updates of k=4 of N=16 streams. §4.2 is accurate.
- **MOPD** (2606.30406) confirms MOPD beating a Param-Merge baseline; the
  specific 0.33–0.86 vs 0.94 normalized scores were not located in the
  abstract and remain unverified.

### 12.3 Consequence for the gates

**G1 is not currently decidable as written.** It measures mHC's *cost* and
takes its *benefit* from sources that, per C1 and C2, measure something else
(mHC vs HC) and never state the iteration count the cheap fix depends on. A
gate that prices only one side of a trade cannot resolve it.

Recommended replacement, which also satisfies §3.1 requirement 4:

1. **Set `use_mhc=False` as the v7 default now.** `presets.py:43` currently
   ships it on, carrying the unresolved `presets.py:38-42` NaN/gradient-
   amplification warning into any trunk run. The measured costs are real and
   three-fold: 36 Sinkhorn projections per forward; a residual stream that is
   genuinely `(B, S, n_hc, D)` for all 18 effective layers (`model.py:1705`,
   expanded `:1948`, collapsed `:1885`), i.e. 4× activation width at every
   checkpoint boundary; and an unquantified stability risk. The benefit
   against no-mHC is unmeasured at any scale, by anyone.
2. **~~Give mHC one G3 ladder slot~~ — withdrawn. Decided off, no slot.**
   *(User decision, 2026-08-12: mHC off permanently, driven by §12.7's
   inference-speed requirement.)* The slot was proposed as insurance against
   the drop being wrong. Under requirement 5 the result could not change the
   decision — mHC is a decode tax on the exact axes decode is bound by
   (§12.7) — so the measurement has no decision value and should not be paid
   for. Spend the slot on the expert re-grain or loops × blocks instead.
3. **Do no Sinkhorn optimisation work.** Cutting iterations or swapping in
   row-softmax mixing is engineering spent on a component whose benefit has
   never been measured — the exact inversion of this repo's own rule.
   §7.3-C's ordering (cheapen first, remove last) is void.

Honest limit on the general claim: a null result at 150M would not have proven
mHC worthless at 600M, and its claimed benefit lives at 27B where this project
cannot measure. That asymmetry argued for dropping rather than keeping — a
feature whose benefit is unmeasurable at our scale and whose cost is
measurable at our scale is one we cannot afford to carry through a trunk run.
The §12.7 requirement then made the question moot.

### 12.4 One material omission

**§7.4's Engram sequencing constraint is dissolved by a paper already in §11.**
The roadmap defers Engram until after the tokenizer decision because "N-gram
IDs are tokenizer-dependent". But 2607.29065, *"Tokenizer-Agnostic Engram
Module"* (31 Jul 2026) — listed in Sources and never used — replaces Engram's
XOR token-hashing with polynomial hashing over byte sequences, achieving
"hash equivalence for byte-equivalent token sequences" at comparable
performance.

If that holds, **G2 and the Engram ladder slot are independent and can run in
parallel**, removing a serialisation from the critical path.

Separately, §5.2 omits two things the K3 abstract leads with: K3 is **natively
multimodal**, and it is built on "Kimi Delta Attention and **Attention
Residuals**, which improve information flow across sequence length and model
**depth**." A depth-wise information-flow mechanism is the most OSRT-adjacent
idea in the report — the recursion is precisely a depth mechanism — and the
scan does not mention it. Worth a read before G3/G4 fix the loops × blocks
design.

### 12.5 Sizing note: the ~1B / 14-expert option (2026-08-12)

Asked whether v7 could be a 1B model with 14 experts. It can, and it lands
almost exactly on 1B. All figures below are generated by
`scripts/compute_budget.py --override … ` on top of the canonical preset (the
only trusted source per `CLAUDE.md`), at vocab 65,536 and `use_mhc=True`:

| shape (per block) | physical | active/token | density |
|---|---|---|---|
| v6 today — 8 × h3840 top-2 | 601,444,393 | 278,217,769 | 25% |
| §8 strawman re-grain — 32 × h960 top-8 | 601,554,985 | 278,328,361 | 25% |
| **14 × h4224 top-2** | **994,294,825** | **288,862,249** | 14.3% |
| 14 × h4352 top-2 | 1,019,067,433 | 292,401,193 | 14.3% |
| 14 × h4224 top-3 | 994,294,825 | 347,254,825 | 21.4% |
| 14 × h4224 top-4 | 994,294,825 | 405,647,401 | 28.6% |
| 28 × h2112 top-4 | 994,359,337 | 288,926,761 | 14.3% |
| 56 × h1088 top-8 | 1,019,260,969 | 292,594,729 | 14.3% |

**Shape gotcha.** `model.py:149` runs `hidden = 64 * ((hidden + 63) // 64)`
for tensor-core alignment. Any `expert_hidden` that is not a multiple of 64 is
silently rounded up — h=1056 builds as h=1088 — and the relative tax grows the
finer the slice. Check the `cfg:` echo line, not the requested value.

**What the trade actually is.** Active rises 278M → 289M (+4%); physical rises
601M → 994M (+65%). Density falls 25% → 14.3%. The case for it is real and is
§6.2's: Inkling-Small beats its own 975B parent on agentic/skill evals while
losing 23 points on SimpleQA — small-active models trade **knowledge, not
skill**, and more total at fixed active is how that knowledge is bought
cheaply. Every 2026 family scanned in §4–§6 moved this way.

**Memory (estimate, not measured).** Optimiser + weights floor goes ~7.6GB →
**~12.3GB** (fp32 master + fp32 grad + fp32 Muon momentum on 2D hidden +
AdamW on embeddings). Activations move little: top-2 is unchanged and expert
width is +10%. Projected peak ~58 → ~63GB — inside an H100 80GB but spending
most of the headroom §3.1.1 asked for; roomy on the RTX PRO 6000 96GB. This
projection has **not** been measured on hardware.

**What should decide it — and it is not settled.** Against *active* params,
994M/A289M wants ~5.8B tokens for 1× Chinchilla; the base is at ~5.6B, so the
requirement is essentially unchanged and the size increase is nearly free.
Against *physical* params it goes from 0.47× to **0.28×** Chinchilla, a gap of
~14B tokens ≈ seven further months of drip at §2.4's rate. Which yardstick
applies to a 6×-weight-reused sparse model is precisely the §2.3.2 open
question that G3's ladder exists to answer.

**Recommendations.**

1. **Prefer 28 × h2112 top-4 over 14 × h4224 top-2.** Identical physical,
   identical active, identical density — but 14 experts at h4224 makes each
   expert *larger* than v6's h3840, which reverses §3.1.2's "more, smaller
   experts" requirement instead of satisfying it. The re-slice also keeps
   adaptive top-k un-deferred on the same terms as §7.3-A.
2. **Do not commit the size increase ahead of G3.** Add one ladder arm —
   *same active, ~1.65× total* — which isolates exactly the question above at
   near-zero marginal cost, since the ladder runs regardless. Committing
   first risks deepening the §2.2 verdict (an undertrained base) rather than
   fixing it: capacity that cannot be filled is not capability.

### 12.6 Unrelated defect found during this audit

`paper.tex:241` states: *"The model was pretrained on 1.2 Trillion tokens."*
The documented figure is ~2.2B at SFT v1/v2 and ~5.6B after midtrain3 — an
overstatement of roughly 200–550×. This is a tracked file. Fix before the
paper is shown to anyone.

---

## 13. North star: highest quality at fastest inference, on Blackwell (2026-08-12)

**Status:** direction set by the user, superseding parts of §7.7, §9 and §10.
This is a **requirements change**, not an audit finding — §12 above remains a
record of what the 2026-08-11 note got right and wrong.

### 13.1 Requirement 5 (amends §3.1)

> **5. Highest quality at the fastest achievable inference.** The deployment
> target is Blackwell: **primarily the RTX PRO 6000 Blackwell** (GB202,
> 96GB GDDR7, single-GPU, no NVLink), with occasional access to B200 /
> GB200 (HBM3e). **Decode latency is a first-class design constraint**, not a
> Tier-4 deployment concern.

Consequence for §3.1.1: the single-GPU envelope is now doubly binding — the
primary card has no NVLink, so single-GPU training is a hardware fact, not a
budget preference. B200/GB200 change training *scale*, not the architecture:
being ~8 TB/s HBM3e they sit even further from bandwidth-bound than the
RTX PRO 6000, so one latency-first design serves both.

### 13.2 Hardware facts (verified 2026-08-12)

The RTX PRO 6000 Blackwell is the **full GB202** with 128 MB L2 and
**fifth-generation Tensor Cores with native FP4/FP6**, plus a second-generation
FP8 Transformer Engine. NVIDIA quotes ~4 PFLOPS FP4 / 2 PFLOPS FP8 /
1 PFLOP BF16-FP16. **FP8 and NVFP4 training are both supported through
Transformer Engine.**

Temper the headline: reported *training* speedups are ~20–50% for FP8 with
roughly a further ~20% for NVFP4 — **not** the 4× the PFLOPS ratio implies.

### 13.3 §10's NVFP4 rejection is void

§10 rejects NVFP4/FP8 pretraining as "Blackwell-only". That was written
against an H100 assumption; under requirement 5 the constraint is satisfied by
the primary card. NVFP4 pretraining is production-proven at scale — Nemotron
Super was the first model pretrained in NVFP4 (25T tokens), Ultra followed
(20T), recipe in arXiv 2509.25149 (keep the last ~15% of layers plus QKV/out
projections high-precision; Hadamard transforms; stochastic rounding). All
verified in §12.2.

**A 20–50% throughput gain attacks the diagnosed bottleneck (tokens, §3)
more directly than any Tier-1 or Tier-2 item in this note.** It should be
priced before the architecture candidates, not after.

The *separate* FP8 objection in §12 of the 2026-07-26 note — `_grouped_mm`
plus shape economics — is **not** void. See G7.

### 13.4 The roofline finding: decode is depth-bound, not bandwidth-bound

At 278M active in BF16 a decode step moves ~0.56 GB of weights per token. On a
~1.8 TB/s part that is a **~3,200 tok/s weight-bandwidth roofline**. The
measured post-optimisation figure is ~136 tok/s — **roughly 4% of roofline**.

*(Caveat: 136 tok/s is the headline number from the decode-perf work; confirm
which card that run used before quoting the ratio as an RTX PRO 6000 result.
The conclusion is robust to the card — at this active-parameter count no
current target is close to bandwidth-bound.)*

Two consequences, one of them counter-intuitive:

1. **FP4 weights are not the decode-speed lever.** They lift the roofline to
   ~13,000 tok/s, and we are nowhere near either figure. FP4's value here is
   **training tokens and memory footprint**, not decode latency.
2. **What binds is sequential depth and launch count** — 18 strictly serial
   effective layers per token, each with attention, MoE dispatch and (until
   now) two Sinkhorn projections.

### 13.5 Reordered speed levers (batch-1 decode)

1. **Speculative decoding** — the direct attack on serial depth. Acceptance
   rate **has never been measured** (§2.3.5); Nemotron reports ~97% two-token
   acceptance from an MTP head. Highest expected value of anything in this
   section. **See §15** — the DSpark result reshapes this item: the draft
   should be *parallel* (MTP-style) with a lightweight sequential head, not
   the autoregressive loop-3 draft §7.7 proposes, and the drafter trains
   against a frozen target so it can be measured on v6 now.
2. **Loop trimming / early exit** — the cross-loop probe puts block 0's
   adjacent-loop CKA movement at 0.30 → 0.17 → 0.07 → **0.05 → 0.05**: loops 5
   and 6 barely move the representation. 17–33% off the depth term.
3. **Megakernel / persistent-kernel decode** — the canonical fix for a
   launch-bound small model, already in the deferred-options backlog. Likely
   where much of the 24× gap to roofline sits.
4. **mHC off** — decided (§12.3); removes 720 Sinkhorn iterations and 4×
   residual-stream traffic per token.
5. **Coarse experts over fine at batch 1** — see the conflict in §13.7.

**Promotions out of Tier 4 (§7.7):** items 1–3 above, plus per-loop
self-distillation to raise acceptance (§14.5) and the early-exit harvest
(§14.6), stop being deployment polish and become headline v7 work.

### 13.6 New gate G7 — the precision path

| gate | question | method | cost | decision rule |
|---|---|---|---|---|
| **G7** | Do the routed experts get low-precision kernels? | Check FP8/NVFP4 support through `torch._grouped_mm` (or a replacement grouped-GEMM) on GB202; benchmark expert-path throughput BF16 vs FP8 vs NVFP4 | hours | If grouped-GEMM stays BF16, the NVFP4 pretrain win applies to <⅓ of parameters and §13.3's priority must be re-priced |

**Why this gates everything:** routed experts are **424.7M of 601M physical
params (71%)** and they run through `torch._grouped_mm`. Transformer Engine
covers dense and attention paths cleanly; the expert path is the open
question. G7 also settles the private-API portability risk flagged in the
2026-07-16 review — the same dependency, one measurement.

Run G7 **before** G3/G4, since it changes what the ladder should be measuring.

### 13.7 Where quality and speed actually conflict

Mostly they do not. NVFP4 training buys quality (tokens); speculative decode,
loop trimming and megakernels buy speed; neither touches the other. Two real
trades remain:

- **Loop count** — the recursion is both the quality thesis and the dominant
  latency term. G4 already prices it; it now carries a decode axis too.
- **Expert granularity** — finer is better for quality-per-FLOP (§7.3-A,
  §3.1.2) but worse for batch-1 GEMM shape. Measurable on the existing decode
  harness with random weights, since throughput does not depend on weight
  quality. Paired same-GPU.

### 13.8 Consequence for §12.5's 1B question

Because decode is **not** bandwidth-bound, extra *total* parameters at fixed
*active* are close to free at inference — only top-k experts are read per
token. §12.5's recommendation to park the size increase behind G3 therefore
stands **on training-token grounds only**; the inference objection does not
exist. If G3 shows the token requirement tracks active rather than total, the
~1B shape becomes materially more attractive under requirement 5, not less.

---

## 14. Revised v7 shape — committed (2026-08-12)

**Supersedes the §8 strawman.** §8 was explicitly "for the ladder to confirm,
not a commitment". This section is a commitment, taken with the token
yardstick still unresolved; §14.7 is the arm that resolves it and §14.9
records the assumption so it stays falsifiable.

### 14.1 The shape

```
dim 1536, 3 physical blocks x 6 loops (18 effective layers)   [unchanged]
GQA 24q/8kv head 64, KDV compressed-latent cache, QK-norm     [unchanged]
RoPE @ 4096 ctx (8K-capable)                                  [unchanged]
Routed experts: 28 x h2112 per block, top-4 (14.3% density)   [RE-GRAINED]
Shared expert:  1 x h2816 per block                           [unchanged]
Router: sqrt(softplus) + Quantile-Balancing bias + z-loss
        + seq-balance 1e-4                                    [QB REQUIRED]
HRA rank 256, native, per effective layer                     [unchanged]
mHC: OFF                                                      [DECIDED, 12.3]
SiTU-GLU experts; per-head Muon; V4 Muon recipe               [graduated]
MTP: 2 heads today; head COUNT reopened by §15 (do not slim to 1) [open]
Tokenizer: 65,536 today; SmolLM3 49,152 pending G2            [open]
```

`h2112 = 33 x 64`, so it survives the `model.py:149` tensor-core round-up
unchanged. Check the `cfg:` echo line whenever this is edited.

### 14.2 Parameter budget (generated, `compute_budget.py --override`)

| component | v6 as built | v7 committed |
|---|---|---|
| embedding | 100,690,944 | 100,690,944 |
| attention | 17,308,032 | 17,308,032 |
| mhc | 921,766 | **0** (off) |
| shared_expert | 38,928,384 | 38,928,384 |
| routed_experts | 424,673,280 | **817,496,064** |
| router | 36,867 | 129,027 |
| adapters | 14,155,776 | 14,155,776 |
| mtp_heads | 4,721,664 | 4,721,664 |
| norms_misc | 7,680 | 7,680 |
| **TOTAL PHYSICAL** | **601,444,393** | **993,437,571** |
| **ACTIVE / TOKEN** | **278,217,769** | **288,004,995** |
| density | 25% | 14.3% |

Physical +65%, active **+3.5%**. If G2 swaps to a 49,152 tokenizer this
becomes **968,271,747 / 262,839,171** — note active falls *below* v6, because
the tied head is fully active.

### 14.3 Why this shape and not the alternatives

- **vs `14 x h4224 top-2`** (identical params and active): rejected because
  h4224 makes every expert *larger* than v6's h3840 — more experts but coarser
  granularity, which reverses requirement 3.1.2 rather than satisfying it.
- **vs the §8 strawman `32 x h960 top-8`**: that was an iso-param re-grain at
  601M. This carries the same fine-graining thesis to ~1B, where the extra
  capacity is bought in total params rather than taken out of the experts.
- **vs `56 x h1088 top-8`**: identical density and identical per-expert token
  exposure, but 8 GEMMs per token instead of 4 at batch-1 decode. Under
  requirement 5 the coarser of two equivalent slices wins until G7 says
  otherwise.

### 14.4 Memory envelope

Optimiser + weights floor (fp32 master + fp32 grad + fp32 Muon momentum on 2D
hidden + AdamW on embeddings):

| | v6 as built | v7 committed |
|---|---|---|
| floor | 7.62 GB | **12.32 GB** |

The +4.7 GB is partly offset on the activation side by mHC being off — the
residual stream drops from `(B, S, n_hc, D)` back to `(B, S, D)`, i.e. 4x
narrower at every checkpoint boundary across all 18 effective layers.
Projected peak lands roughly in the 58-63 GB band at batch 6 / seq 4096,
against a **96 GB** primary card (requirement 5). **Not measured** — confirm
before the trunk run.

Note the §3.1.1 "≤~60GB" target was written when an H100 80GB was co-primary.
Under requirement 5 the binding card is the RTX PRO 6000 at 96 GB, where this
shape sits at roughly 60-66% utilisation.

### 14.5 Why fine-graining is more affordable in a recursive model

Each block's expert pool sees the token stream once per loop, so weight reuse
multiplies expert exposure sixfold. Over a 5.6B-token run:

| shape | per-expert token exposure |
|---|---|
| v6 — 8 experts top-2 | 2.80B |
| **v7 — 28 experts top-4** | **1.60B** |
| 56 experts top-8 | 1.60B |

A halving, not a collapse — and it is the recursion that pays for it. This
argument is specific to OSRT and is not made anywhere in §§4-7.

For context, 14.3% density is **conservative** by 2026 standards: V4-Flash
runs 6/256 (2.3%), Kimi K3 16/896 (1.8%). This shape is nowhere near the
sparsity frontier.

### 14.6 Required consequences

1. **Quantile Balancing is promoted from Tier-1 candidate (item 1.4) to
   required.** At E=28 one dead expert costs 3.6% of block capacity — small
   enough to hide in the loss curve, large enough to matter. K3's QB sets each
   expert bias from the router-score quantile matching its target load:
   deterministic, hyperparameter-free, "zero dead experts". The current ±γ
   heuristic was tuned at E=8.
2. **Fold the `E=28 / h2112` grouped-GEMM benchmark into G7.** That gate
   already visits the expert path for FP8/NVFP4; measuring 28 medium GEMMs
   against v6's 8 large ones on the same visit is free.
3. **Per-loop routing telemetry** (already built) becomes load-bearing —
   collapse detection matters more with 3.5x the experts.

### 14.7 G3a — token-yardstick arm (new sub-gate, blocks the trunk run)

| gate | question | method | cost | decision rule |
|---|---|---|---|---|
| **G3a** | Does the token requirement track *active* or *total* params? | Ladder: fix active at ~150M, sweep total across ~150M / 300M / 500M by varying expert count at fixed top-k FLOPs, hold the token budget constant, compare loss-per-token | ~3 runs, days on the always-on box | If loss-per-token is flat across the sweep, the yardstick is active and 14.1 is safe. If it degrades with total, re-price 14.1 against the drip budget before the trunk run. |

This is the single largest uncertainty in the plan and it does **not** require
the full G3 ladder. Run it first. On active params this shape sits at ~1x
Chinchilla, unchanged from v6; on physical it sits at 0.28x against v6's
0.47x, and v6's diagnosed failure (§2.2) was an undertrained base.

### 14.8 Recorded assumption

> **v7 assumes the compute-optimal token requirement tracks *active*
> parameters, not total.** This follows from `C = 6ND` with N set by active
> FLOPs, and is consistent with 2026 practice (every scanned family is far
> sparser than this shape at far larger token budgets). It is **falsified** if
> G3a shows loss-per-token degrading as total params rise at fixed active, or
> if the trunk run's loss curve trails a 601M control at matched tokens.

Carry this sentence into `presets.py` beside the re-grain so it stays testable
rather than becoming folklore.

---

## 15. DSpark — speculative-decoding evidence (2026-08-12)

**Source:** *DSpark: Confidence-Scheduled Speculative Decoding with
Semi-Autoregressive Generation*, arXiv:2607.05147v1, 6 Jul 2026. DeepSeek-AI +
Peking University — same group as the mHC (2512.24880) and Engram
(2601.07372) papers. Not vendored — arXiv's default licence does not grant redistribution
rights. Fetch to `research/` locally (gitignored).

**Why it matters here:** §13.5 ranks speculative decoding as the highest-value
speed lever, and the baseline DSpark displaces is **MTP-1** — DeepSeek-V4's
production draft setup and precisely what OSRT's spec-decode plan is. DSpark
replaced it in production two weeks after the V4-preview release.

### 15.1 Results (Table 1 — accepted length τ per round, scheduler disabled)

| target | drafter | GSM8K | MATH | HumanEval | MT-Bench |
|---|---|---|---|---|---|
| Qwen3-4B | Eagle3 (autoregressive) | 5.14 | 4.62 | 4.16 | 2.39 |
| Qwen3-4B | DFlash (parallel) | 5.40 | 4.85 | 4.74 | 3.07 |
| **Qwen3-4B** | **DSpark** | **6.11** | **5.70** | **5.38** | **3.64** |
| Qwen3-8B | DSpark | 6.17 | 5.78 | 5.52 | 3.72 |
| Qwen3-14B | DSpark | 6.21 | 5.74 | 5.43 | 3.70 |

Macro-average gain over Eagle3: +30.9% / +26.7% / +30.0% at 4B / 8B / 14B;
over DFlash: +16.3% / +18.4% / +18.3%. Generalises across families
(Gemma4-12B). Live traffic vs MTP-1: **60–85% faster per-user generation at
matched throughput** on V4-Flash (57–78% on V4-Pro).

Two observations that matter for OSRT:

- **τ is flat from 4B to 14B** (GSM8K 6.11 / 6.17 / 6.21) — scale-insensitive
  across that range. **Not demonstrated below 4B**, so extrapolating to ~1B is
  an assumption, not a result.
- **Math is the best domain** (τ ≈ 5.7–6.2), open-ended chat the worst
  (≈ 3.5). OSRT's target task sits in the favourable half.

### 15.2 Correction 1 — do NOT slim MTP to one head

§7.2 item 1.6 and the old §8 strawman both move toward a single MTP head. The
paper states outright *why* DeepSeek ran MTP-1 in production:

> "deploying a static multi-token drafter (e.g., MTP-3/5) strictly degrades
> aggregate throughput under high concurrency due to excessive verification
> overhead."

That is a **high-concurrency serving constraint OSRT does not have.** Under
requirement 5 (§13.1) the design point is batch-1 latency, and the paper's own
framing is that verifying extra tokens is *"nearly free under light loads"* and
that low-confidence verification *"incurs minimal opportunity cost under low
concurrency"*.

**Consequence:** more draft positions plus a cheap sequential head is the
right shape for OSRT, not fewer. §14.1's MTP line is reopened accordingly.

### 15.3 Correction 2 — §7.7's loop-3 draft backs the losing family

§7.7 plans "spec-decode via the loop-3 draft", which is an **autoregressive**
drafter. In Table 1 the autoregressive SOTA (Eagle3) loses to *both* parallel
drafters at every scale and every domain. The paper's §4.3.1 treats this as a
deliberate finding, not noise.

OSRT's MTP heads are already a parallel multi-position drafter. The lever is
to add DSpark's lightweight sequential correction on top of them, not to build
a sequential drafter out of the loop stack.

### 15.4 The property that makes this immediately actionable

> "The target model is frozen throughout training; the draft model shares its
> embedding layer and language modeling head and keeps them frozen, updating
> only the backbone drafter, sequential block, and confidence head."

So the drafter is trained **post-hoc against a frozen target**. Two
consequences:

1. It touches **nothing** in the v7 pretraining plan — it is orthogonal to
   G2/G3/G3a/G7.
2. It can run against the **existing v6 checkpoint today**, which would
   finally measure the acceptance rate §2.3.5 records as never measured.

### 15.5 Cost and artifacts

- **Training data:** Open-PerfectBlend, 1.3M samples — prompts only, responses
  regenerated by the target model. 10 epochs per drafter.
- **Drafter size:** 5 layers for DSpark/DFlash in the main results, but Figure
  3 shows a **2-layer DSpark beating a 5-layer DFlash** — sequential modelling
  is parameter-efficient. The Markov head costs ~1.3% latency.
- **Objective:** `L = 0.1·L_ce + 0.9·L_tv + 1.0·L_conf`, position-weighted by
  `w_k = exp(-(k-1)/γ)`. `L_tv` is a *direct* proxy for acceptance — per-step
  acceptance probability is `1 − ½‖p_d − p_t‖₁`, so minimising it maximises
  expected acceptance. This is a cleaner training signal than the CE-only
  framing in §14.5 of the 2026-07-26 note.
- **Released:** DSpark checkpoints plus **DeepSpec**, a training repository
  containing Eagle3, DFlash and DSpark implementations.

### 15.6 Caveats

- **Nothing here has been run on a depth-recurrent model.** The interaction
  between a γ-token draft block and 18 sequential effective layers is
  unexplored; the draft's own cost profile differs when the target reuses
  weights across loops.
- **The Markov head is not free.** It is a low-rank factorisation of a V×V
  transition matrix, so roughly `2·V·r` parameters — at V=65,536 and r=256
  (the paper's default) that is **~33.5M**, ≈3.4% of the §14 shape; ~25M at a
  49,152 vocab. *Estimated from the stated formula, not a quoted figure.*
- **The confidence-scheduler half of the paper is largely irrelevant to us.**
  Its gains are high-concurrency serving gains. The draft-quality half
  (Table 1, scheduler disabled) is the part that transfers to batch-1 decode.
- τ below 4B is unmeasured, and the custom 65K tokenizer is a further
  untested variable until G2 resolves.

### 15.7 G8 — drafter acceptance on frozen v6 (parallel, blocks nothing)

| gate | question | method | cost | decision rule |
|---|---|---|---|---|
| **G8** | What accepted length does OSRT actually sustain, and does the semi-autoregressive correction help on a recursive target? | Train a small parallel drafter against the **frozen** v6 checkpoint (MTP-style, γ ∈ {3,5}), then add a Markov head; measure τ on GSM8K, held out from the §12.5 dev panel | days; no trunk-run dependency | Establishes the τ that §2.3.5 has never measured. If τ > 2 the decode plan changes materially; if the Markov head adds nothing on a recursive target, that is itself a novel negative result worth recording. |

**This is the cheapest real experiment currently available.** It runs against
a checkpoint that already exists, needs no architecture decision, and is not
blocked by G7 or G3a. It also converts §13.5's top-ranked lever from an
assumption into a number.
